import React from 'react';
import ColorSystem from './ColorSystem';

export default {
    title: 'Components/Color System',
    component: ColorSystem,
};

const Template = () => <ColorSystem />;

export const Default = Template.bind({});
Default.args = {};